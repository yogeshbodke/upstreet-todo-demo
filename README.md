# upstreet-todo-demo — Snake

Classic Nokia 6600-style Snake II game. Single self-contained static HTML
(canvas + JS, retro olive-green LCD, on-screen D-pad, localStorage high score),
served by nginx. Deployed to a live Upstreet playground via the MCP.
