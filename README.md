# upstreet-todo-demo

Now hosting a classic **Nokia 6600-style Snake II** game — single self-contained
static HTML (canvas + JS, retro olive-green LCD, on-screen D-pad, localStorage
high score), served by nginx. Deployed to a live Upstreet playground via the MCP
(public-URL onboarding). Previous To-Do demo preserved in git history.
